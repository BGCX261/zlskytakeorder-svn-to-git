更改事项：
1、默认列表撑到5行左右；
2、有remark的才增加*点后显示详细信息；
3、Info改为I、view改为v点后显示详细信息；
4、桌号、客人号缩为2位，操作员改图标和显示框；
5、统一输入风格，键盘和输入框；
6、点菜时增加操作员号；
7、增加一个图标表示手动输入菜号和remark号；
8、增加开关是否允许分桌，有分桌rehold时需要显示列表让操作员选择；



9.3
测试存在的问题：
1.选菜时，没有指定哪号人选菜，用默认1号人选菜时，提交后列表里不能显示刚才所选的菜。
2.点列表项的“*”号时，不会显示这道菜的Remark信息，I下的“V”也不会显示这道菜的详细信息（包括价格信息等）
3.Remark选完后，Remark选项的div层不会自动隐藏。
4.提示信息都要隐藏掉